{% extends 'layouts/base.html.twig' %}

{% block title %}{{ city }}{% endblock %}

{% block body %}

{#  @TODO faire le rendu sous forme de tableau.  #}
    <div style="height: 120px;"></div>
    <p> {{ city.id }}</p>
    <p> {{ city.name }}</p>

{% endblock %}